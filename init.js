// ============================================================
        // INITIAL RENDER + AUTO-SAVE
        // ============================================================
        render();
        setInterval(saveDB, 3000);

